<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jitendrakumar.incometracker">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.IncomeReportActivity" />
        <activity android:name=".activities.ExpenseReportActivity" />
        <activity android:name=".activities.TodoActivity" />
        <activity android:name=".activities.BarChartActivity" />
        <activity android:name=".activities.ExpenseBarchartActivity" />
        <activity android:name=".activities.SavingBarchartActivity" />
        <activity android:name=".activities.IncomePiechartActivity"></activity>
    </application>

</manifest>